//
//  ColorsExtension.swift
//  OneOnOne
//
//  Created by Vlad on 9/10/24.
//

import SwiftUI

extension Color {
    
    // MARK: - Mode Colors
    static let appBackgroundColor = Color("appBackgroundColor") // #FFFFFF (White)
    static let appPrimaryTextColor = Color("appPrimaryTextColor") // #333333 (Dark Gray)
    static let appSecondaryTextColor = Color("appSecondaryTextColor") // #7D7D7D (Medium Gray)
    static let appAccentColor = Color("appAccentColor") // #FFA500 (Orange)
    static let appButtonBackgroundColor = Color("appButtonBackgroundColor") // #F0F0F0 (Light Gray)
    static let appButtonTextColor = Color("appButtonTextColor") // #FFA500 (Orange)
    static let appSeparatorColor = Color("appSeparatorColor") // #E0E0E0 (Very Light Gray)
    
    // MARK: - Shared Colors (Light & Dark)
    static let appErrorRedColor = Color("appErrorRedColor") // #FF3B30 (Red)
    static let appSuccessGreenColor = Color("appSuccessGreenColor") // #4CD964 (Green)
    static let appWarningYellowColor = Color("appWarningYellowColor") // #FFCC00 (Yellow)
    static let appInfoBlueColor = Color("appInfoBlueColor") // #5AC8FA (Light Blue)
}
